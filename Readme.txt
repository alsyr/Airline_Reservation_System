Each passenger has 5 elements

the class = a string that can be either “first” or “economy”
the row = an int between [0,1] for first class and [0,19] for economy class
the seat = an int between [0,3] for first class and [0,5] for economy class
the name = a string
the group’s name = a string (“NONE” for independent passengers)